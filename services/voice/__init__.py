"""
services.voice — Speechma text-to-speech integration (later phase).
Speechma API key stays server-side; the frontend only ever receives audio
back from a backend endpoint, never the key itself.
"""
